{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:seed": "tsx prisma/seed.ts",
    "db:studio": "prisma studio",
    "postinstall": "pnpm run db:migrate && pnpm run db:generate && cd ../.. && make seed-db-docker || make seed-db"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/helmet": "^12.0.1",
    "@prisma/client": "^5.21.0",
    "dayjs": "^1.11.13",
    "dotenv": "^16.4.5",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "prisma": "^5.21.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.1",
    "typescript": "^5.5.3"
  }
}
